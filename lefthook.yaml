pre-commit:
  parallel: true
  commands:
    eslint:
      run: pnpm lint
    prettier:
      run: pnpm format
    typecheck:
      run: pnpm typecheck

pre-push:
  parallel: true
  commands:
    build:
      run: pnpm build
    build-compose:
      run: docker compose build