{
  "name": "@mse/database",
  "version": "0.0.1",
  "private": true,
  "description": "MSE database.",
  "scripts": {
    "lint": "eslint --cache src",
    "typecheck": "pnpm run prisma:generate && tsc --noEmit -p tsconfig.json",
    "format": "prettier --cache --write src",
    "format:check": "prettier --cache --check src",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma deploy",
    "smoke": "tsx src/scripts/hello_world.ts",
    "dev": "tsx src/scripts/hello_world.ts"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.4.1",
    "@prisma/client": "^7.4.1"
  },
  "devDependencies": {
    "dotenv": "^17.3.1",
    "prisma": "^7.4.1"
  }
}
